# =============================================================================
# DiaVeritas — Python Dependencies
#
# Install with:  py -m pip install -r requirements.txt
#
# NOTE: Large models (sentence-transformers, transformers, etc.) will be
# downloaded on first use. Estimated download size: ~2–4 GB depending on
# chosen models.  Run in an environment with stable internet.
# =============================================================================

# ---------------------------------------------------------------------------
# Core Python utilities
# ---------------------------------------------------------------------------
python-dotenv>=1.0.0          # Load .env config
pydantic>=2.0.0               # Data validation / config models
pydantic-settings>=2.0.0      # Settings from env
tqdm>=4.65.0                  # Progress bars
loguru>=0.7.0                 # Structured logging (simpler than stdlib logging)
click>=8.1.0                  # CLI entry points

# ---------------------------------------------------------------------------
# Data handling
# ---------------------------------------------------------------------------
pandas>=2.0.0
numpy>=1.26.0
scipy>=1.11.0

# ---------------------------------------------------------------------------
# Corpus collection (PubMed / PMC)
# ---------------------------------------------------------------------------
biopython>=1.83               # Entrez API for PubMed queries
requests>=2.31.0
lxml>=5.0.0                   # XML parsing for PMC full-text

# ---------------------------------------------------------------------------
# PDF parsing (fallback)
# ---------------------------------------------------------------------------
pymupdf>=1.24.0               # PyMuPDF: fast, reliable PDF text extraction

# ---------------------------------------------------------------------------
# Biomedical NLP
# ---------------------------------------------------------------------------
spacy>=3.8.0                  # Already installed
scispacy>=0.5.5               # Biomedical spaCy extensions
# scispacy model installed separately:
#   py -m pip install https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.5/en_core_sci_sm-0.5.5.tar.gz

# ---------------------------------------------------------------------------
# Embeddings & Transformers
# ---------------------------------------------------------------------------
transformers>=4.40.0
sentence-transformers>=3.0.0
torch>=2.0.0                  # CPU build already installed; keep as-is

# ---------------------------------------------------------------------------
# Vector store
# ---------------------------------------------------------------------------
chromadb>=0.5.0               # Primary vector store (pure Python, no C++ pain)
# faiss-cpu>=1.8.0            # Alternative; uncomment if ChromaDB is too slow

# ---------------------------------------------------------------------------
# BM25 lexical retrieval
# ---------------------------------------------------------------------------
rank-bm25>=0.2.2

# ---------------------------------------------------------------------------
# Reranking
# ---------------------------------------------------------------------------
# cross-encoder is part of sentence-transformers; no extra install needed

# ---------------------------------------------------------------------------
# LLM clients
# ---------------------------------------------------------------------------
openai>=1.30.0                # OpenAI + Groq (Groq is OpenAI-compatible)
anthropic>=0.25.0             # Anthropic Claude
google-generativeai>=0.5.0    # Google Gemini

# ---------------------------------------------------------------------------
# Web / API layer
# ---------------------------------------------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0

# ---------------------------------------------------------------------------
# UI
# ---------------------------------------------------------------------------
streamlit>=1.35.0             # Already installed

# ---------------------------------------------------------------------------
# Evaluation / Metrics
# ---------------------------------------------------------------------------
scikit-learn>=1.5.0           # Already installed (precision/recall/F1)
rouge-score>=0.1.2            # ROUGE for text summarization evaluation
nltk>=3.8.0                   # Tokenization utilities

# ---------------------------------------------------------------------------
# Notebook / Exploration
# ---------------------------------------------------------------------------
ipykernel>=6.29.0             # Already installed
matplotlib>=3.8.0             # Already installed
seaborn>=0.13.0               # Already installed
plotly>=5.20.0                # Already installed

# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------
pytest>=8.0.0                 # Already installed
pytest-asyncio>=0.23.0        # Already installed
